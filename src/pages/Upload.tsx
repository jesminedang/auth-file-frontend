import React from 'react';
import UploadForm from '../components/upload/UploadForm';

const Upload = () => {
  return (
    <div className="p-4 max-w-md mx-auto bg-white items-center justify-center h-screen">
      <UploadForm />
    </div>
  );
};

export default Upload;
